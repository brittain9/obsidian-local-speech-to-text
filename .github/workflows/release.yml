name: release

on:
  workflow_dispatch:
  push:
    tags:
      - '[0-9][0-9][0-9][0-9].*.*'

permissions:
  attestations: write
  contents: write
  id-token: write

# Workflow-level env trims CI log volume. The bulk of the noise was cargo's
# per-crate "Compiling …" lines and npm's audit/fund/info chatter; warnings
# and errors still surface.
env:
  CARGO_TERM_QUIET: 'true'
  CARGO_TERM_PROGRESS_WHEN: never
  NPM_CONFIG_LOGLEVEL: error
  NPM_CONFIG_FUND: 'false'
  NPM_CONFIG_AUDIT: 'false'
  NPM_CONFIG_PROGRESS: 'false'
  DEBIAN_FRONTEND: noninteractive

jobs:
  metadata:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.value }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24.14.1

      - id: version
        name: Resolve release version
        run: echo "value=$(node scripts/read-release-version.mjs)" >> "$GITHUB_OUTPUT"

      - name: Validate pushed tag
        if: github.event_name == 'push'
        run: node scripts/read-release-version.mjs --tag "${GITHUB_REF_NAME}"

  plugin-bundle:
    needs: metadata
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24.14.1
          cache: npm

      - name: Upgrade npm
        run: npm install --global npm@11.12.1

      - name: Install JavaScript dependencies
        run: npm ci

      - name: Build plugin bundle
        run: npm run build:frontend

      - name: Upload plugin bundle
        uses: actions/upload-artifact@v7
        with:
          name: plugin-bundle
          path: |
            main.js
            manifest.json
            styles.css

  build-sidecar-posix:
    needs: metadata
    # GGML_NATIVE=OFF at job scope — prevents AVX-512 bake-in on CI runners and
    # ensures rust-cache key includes it before the build step runs (lessons 2026-04-21).
    env:
      GGML_NATIVE: 'OFF'
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-15
            asset_name: sidecar-macos-arm64
            archive_name: sidecar-macos-arm64.tar.gz
            binary_path: native/target/release/obsidian-local-stt-sidecar
            build_command: npm run build:sidecar:release
            cache_workspaces: native -> target
            cuda: false
          - runner: ubuntu-latest
            asset_name: sidecar-linux-x86_64-cpu
            archive_name: sidecar-linux-x86_64-cpu.tar.gz
            binary_path: native/target/release/obsidian-local-stt-sidecar
            build_command: npm run build:sidecar:release
            cache_workspaces: native -> target
            cuda: false
          - runner: ubuntu-latest
            asset_name: sidecar-linux-x86_64-cuda
            archive_name: sidecar-linux-x86_64-cuda.tar.gz
            binary_path: native/target-cuda/release/obsidian-local-stt-sidecar
            build_command: npm run build:sidecar:cuda:release
            cache_workspaces: native -> target-cuda
            cuda: true

    runs-on: ${{ matrix.runner }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24.14.1

      - name: Install native build dependencies
        if: runner.os == 'Linux'
        run: sudo apt-get -qq update && sudo apt-get install -qq -y cmake

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.94.1

      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: ${{ matrix.cache_workspaces }}
          shared-key: ${{ matrix.asset_name }}
          # GGML and WHISPER added so future GGML_*/WHISPER_* toggles bust the cache key (lessons 2026-04-21).
          env-vars: CARGO CC CFLAGS CXX CMAKE RUST GGML WHISPER
          cache-on-failure: true

      - name: Install CUDA toolkit
        if: matrix.cuda
        uses: Jimver/cuda-toolkit@v0.2.35
        with:
          cuda: '12.9.0'
          method: network

      - name: Build sidecar
        run: ${{ matrix.build_command }}

      - name: Ad-hoc sign macOS binary
        if: runner.os == 'macOS'
        run: codesign --force --sign - ${{ matrix.binary_path }}

      - name: Package release archive
        shell: bash
        env:
          ARCHIVE_NAME: ${{ matrix.archive_name }}
          ASSET_NAME: ${{ matrix.asset_name }}
          BINARY_PATH: ${{ matrix.binary_path }}
          CUDA: ${{ matrix.cuda }}
        run: |
          artifact_dir="dist/${ASSET_NAME}"
          mkdir -p "$artifact_dir"
          cp "$BINARY_PATH" "$artifact_dir/obsidian-local-stt-sidecar"
          if [[ "$CUDA" == "true" ]]; then
            while IFS= read -r extra_file; do
              cp "$(dirname "$BINARY_PATH")/$extra_file" "$artifact_dir/$extra_file"
              # strip --strip-unneeded keeps dynamic symbols; Linux-only (macOS CUDA not a target).
              strip --strip-unneeded "$artifact_dir/$extra_file"
            done < <(node scripts/list-cuda-artifacts.mjs providers linux)
            # Bundle CUDA runtime libs — major-versioned, not forward-compatible (lessons 2026-04-21).
            # cp -L dereferences symlinks. Lib path mirrors scripts/build-cuda.sh.
            cuda_lib="$(dirname "$(dirname "$(which nvcc)")")/targets/$(uname -m)-linux/lib"
            while IFS= read -r runtime_file; do
              cp -L "$cuda_lib/$runtime_file" "$artifact_dir/$runtime_file"
            done < <(node scripts/list-cuda-artifacts.mjs runtime linux)
          fi
          # Linux-only: strip bundled C++/CUDA debug sections. Skip macOS — binary is already codesigned.
          if [[ "$RUNNER_OS" == "Linux" ]]; then
            strip --strip-unneeded "$artifact_dir/obsidian-local-stt-sidecar"
          fi
          tar -czf "dist/${ARCHIVE_NAME}" -C "$artifact_dir" .

      - name: Attest build provenance
        if: github.event_name == 'push'
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: dist/${{ matrix.archive_name }}

      - name: Upload release archive
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.asset_name }}
          path: dist/${{ matrix.archive_name }}

  build-sidecar-windows:
    needs: metadata
    # GGML_NATIVE=OFF at job scope — same rationale as build-sidecar-posix (lessons 2026-04-21).
    env:
      GGML_NATIVE: 'OFF'
    strategy:
      fail-fast: false
      matrix:
        include:
          - asset_name: sidecar-windows-x86_64-cpu
            archive_name: sidecar-windows-x86_64-cpu.zip
            binary_path: native/target/release/obsidian-local-stt-sidecar.exe
            build_command: npm run build:sidecar:release
            cache_workspaces: native -> target
            cuda: false
          - asset_name: sidecar-windows-x86_64-cuda
            archive_name: sidecar-windows-x86_64-cuda.zip
            binary_path: native/target-cuda/release/obsidian-local-stt-sidecar.exe
            build_command: npm run build:sidecar:cuda:windows:release
            cache_workspaces: native -> target-cuda
            cuda: true

    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24.14.1

      - name: Expose libclang
        shell: pwsh
        run: echo "LIBCLANG_PATH=$((Get-Command clang).Source | Split-Path -Parent)" >> $env:GITHUB_ENV

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.94.1

      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: ${{ matrix.cache_workspaces }}
          shared-key: ${{ matrix.asset_name }}
          env-vars: CARGO CC CFLAGS CXX CMAKE RUST GGML WHISPER
          cache-on-failure: true

      - name: Install CUDA toolkit
        if: matrix.cuda
        uses: Jimver/cuda-toolkit@v0.2.35
        with:
          cuda: '12.9.0'
          method: network
          sub-packages: '["nvcc", "cudart", "cublas", "cublas_dev", "cufft", "visual_studio_integration"]'

      - name: Build sidecar
        shell: pwsh
        run: ${{ matrix.build_command }}

      - name: Package release archive
        shell: pwsh
        env:
          ARCHIVE_NAME: ${{ matrix.archive_name }}
          ASSET_NAME: ${{ matrix.asset_name }}
          BINARY_PATH: ${{ matrix.binary_path }}
          CUDA: ${{ matrix.cuda }}
        run: |
          $artifactDir = "dist/$env:ASSET_NAME"
          $binaryDir = Split-Path $env:BINARY_PATH -Parent
          New-Item -ItemType Directory -Force -Path $artifactDir | Out-Null
          Copy-Item $env:BINARY_PATH "$artifactDir/obsidian-local-stt-sidecar.exe"
          if ($env:CUDA -eq 'true') {
            # Explicit provider list — not a blanket *.dll glob (lessons 2026-04-21).
            $providerFiles = (& node scripts/list-cuda-artifacts.mjs providers win32) -split "`r?`n" | Where-Object { $_ -ne '' }
            foreach ($providerFile in $providerFiles) {
              Copy-Item (Join-Path $binaryDir $providerFile) (Join-Path $artifactDir $providerFile)
            }
            # Bundle CUDA runtime DLLs — major-versioned, exe dir wins DLL search order (lessons 2026-04-21).
            $runtimeFiles = (& node scripts/list-cuda-artifacts.mjs runtime win32) -split "`r?`n" | Where-Object { $_ -ne '' }
            foreach ($runtimeFile in $runtimeFiles) {
              $source = Join-Path $env:CUDA_PATH "bin/$runtimeFile"
              Copy-Item $source (Join-Path $artifactDir $runtimeFile)
            }
          }
          Compress-Archive -Path "$artifactDir/*" -DestinationPath "dist/$env:ARCHIVE_NAME" -Force

      - name: Attest build provenance
        if: github.event_name == 'push'
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: dist/${{ matrix.archive_name }}

      - name: Upload release archive
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.asset_name }}
          path: dist/${{ matrix.archive_name }}

  publish:
    needs:
      - metadata
      - plugin-bundle
      - build-sidecar-posix
      - build-sidecar-windows
    runs-on: ubuntu-latest

    steps:
      - name: Download plugin bundle
        uses: actions/download-artifact@v5
        with:
          name: plugin-bundle
          path: dist/plugin-bundle

      - name: Download sidecar archives
        uses: actions/download-artifact@v5
        with:
          pattern: sidecar-*
          path: dist/release
          merge-multiple: true

      - name: Assemble release files
        run: |
          mkdir -p dist/release
          cp dist/plugin-bundle/main.js dist/release/main.js
          cp dist/plugin-bundle/manifest.json dist/release/manifest.json
          cp dist/plugin-bundle/styles.css dist/release/styles.css
          (cd dist/release && find . -maxdepth 1 -type f \( -name '*.tar.gz' -o -name '*.zip' \) -printf '%P\n' \
            | sort \
            | xargs sha256sum > checksums.txt)
          test -f dist/release/main.js
          test -f dist/release/manifest.json
          test -f dist/release/styles.css
          test -s dist/release/checksums.txt

      - name: Upload dry-run bundle
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v7
        with:
          name: release-${{ needs.metadata.outputs.version }}
          path: dist/release/*

      - name: Create draft release
        if: github.event_name == 'push'
        uses: softprops/action-gh-release@v2
        with:
          files: dist/release/*
          name: ${{ needs.metadata.outputs.version }}
          tag_name: ${{ needs.metadata.outputs.version }}
          draft: true

      - name: Publish release
        if: github.event_name == 'push'
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.metadata.outputs.version }}
        run: gh release edit "$TAG" --draft=false --repo "$GITHUB_REPOSITORY"
