- 2026-04-11 | [build] | Pattern: `cargo clippy` can redundantly re-run `whisper-rs-sys` native setup and fail in environments where the real `cargo build` / `cargo test` path already succeeds. | Rule: keep real native verification in `cargo build` and `cargo test`, and run clippy with `DOCS_RS=1` so linting skips the extra whisper.cpp CMake pass. Do NOT also set `WHISPER_DONT_GENERATE_BINDINGS=1` — that forces the shipped (Linux/glibc) bindings and breaks Windows with E0080 overflows on `_G_fpos_t` / `_IO_FILE` size asserts.
- 2026-04-12 | [test] | Pattern: installer `poll_event_returns_failed_on_channel_disconnect` flakes under parallel load (real HTTP, 10s timeout). Passes reliably in isolation. | Rule: rerun once before treating as a regression; fix or quarantine if it persists past 2026-Q3.
- 2026-04-13 | [ui] | Pattern: complex Obsidian UI surfaces (model browser) accumulate bugs when concurrent states aren't enumerated. | Rule: before patching multi-state UI, list allowed states/transitions; prefer Obsidian-native components (D-005) over custom rendering.
- 2026-04-19 | [process] | Pattern: Updating `PLANS.md` just because implementation changed creates extra churn and second-guesses an approved plan artifact. | Rule: when shipping code changes, do not edit `PLANS.md` unless the user explicitly asks for a plan change or the plan itself is being revised.
- 2026-04-19 | [ui] | Pattern: simplifying dictation chrome removed both the placement cue and the backend-progress cue at once, leaving the default `at_cursor` flow with no feedback. | Rule: keep at least one always-visible placement cue and one backend-progress cue when reducing dictation affordances.
- 2026-04-21 | [process] | Pattern: Stacked-branch merge status is easy to misread when a base branch was merged to `main` earlier and then advanced again afterward. | Rule: when confirming whether work reached `main`, compare the specific commit SHA against `origin/main`, not just whether the branch name was previously merged.
- 2026-04-21 | [process] | Pattern: Native Windows crash triage can go sideways fast when a decimal exit code is diagnosed from memory instead of being converted and checked first. | Rule: always convert Windows process exit codes to hex before forming a crash hypothesis, and do not trust an unverified mnemonic.
- 2026-04-21 | [build] | Pattern: the 2026.4.21 Windows release sidecar crashed with `STATUS_ILLEGAL_INSTRUCTION` (0xC000001D / exit 3221225501) on Alder Lake consumer CPUs during the first `whisper_full()` call. Cause: whisper.cpp/ggml defaults `GGML_NATIVE=ON`, which runs `FindSIMD.cmake` on the build host and bakes the host's SIMD level (AVX-512 on current `windows-latest` / `ubuntu-latest` runners) into the shipped binary. Debug builds on the user's own machine probed the user's CPU and stayed at AVX2, hiding the issue in local testing. | Rule: release pipelines that wrap ggml MUST set `GGML_NATIVE=OFF` (and any other native-probing env) so the distributed binary has a runner-independent baseline (AVX2 is ggml's fallback). Never rely on the CI CPU matching the user's CPU. `whisper-rs-sys/build.rs` passes every `GGML_*` / `WHISPER_*` / `CMAKE_*` env through to cmake.
- 2026-04-21 | [build] | Pattern: the first `GGML_NATIVE=OFF` fix landed at step-level `env:` scope, but Swatinem/rust-cache computes its restore key *before* the Build sidecar step runs, so the CPU matrix hit a stale cache from before the flag was added and re-shipped the AVX-512 binary. whisper-rs-sys's `build.rs` never ran `cargo:rerun-if-env-changed=GGML_NATIVE` (the var wasn't set when it originally ran), so cargo happily reused the cached artifacts. | Rule: set build-affecting env vars at job scope, not step scope, so the cache action sees them. Extend `Swatinem/rust-cache`'s `env-vars:` prefix list to cover the relevant namespaces (e.g. `GGML WHISPER` alongside the defaults) so future flips bust the cache key automatically. When in doubt, also `gh cache delete` the stale key.
- 2026-04-21 | [build] | Pattern: the 2026.4.21 CUDA sidecar failed to start on a user's machine with `STATUS_DLL_NOT_FOUND` because the exe directly imports `cudart64_12.dll` / `cublas64_12.dll` but the user had CUDA 13.x installed — CUDA runtime DLLs are major-versioned and not forward-compatible, and on Windows NVIDIA ships no `cublas_static.lib` so static linking isn't an option. On Linux the shipped binary's rpath still pointed at the CI runner's `/usr/local/cuda/lib64/`, so bundling the `.so.12` files alone wouldn't have helped either. | Rule: ship the CUDA runtime DLLs/SOs (`cudart`, `cublas`, `cublasLt`) alongside the binary in CUDA release archives (NVIDIA's EULA Attachment A permits this for those three files), and set the Linux binary's rpath to `$ORIGIN` so the dynamic linker finds the bundled `.so`s next to the exe instead of a host-specific absolute path.
