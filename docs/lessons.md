- 2026-04-11 | Pattern: `cargo clippy` can redundantly re-run `whisper-rs-sys` native setup and fail in environments where the real `cargo build` / `cargo test` path already succeeds. | Rule: keep real native verification in `cargo build` and `cargo test`, but run clippy with `DOCS_RS=1 WHISPER_DONT_GENERATE_BINDINGS=1` so linting analyzes Rust code without depending on an extra whisper.cpp bindgen/CMake pass.
- 2026-04-12 | Pattern: `installer::tests::poll_event_returns_failed_on_channel_disconnect` is flaky under load — it makes a real HTTP connection and the 10s timeout fails when 56 tests run in parallel. Passes reliably in isolation. | Rule: if this test fails during `npm run check`, rerun before investigating. It's a known race condition, not a regression signal.
- 2026-04-11 | Pattern: Flatpak CUDA setup requires sandbox-specific overrides (`host-os`, `readlink -f`, wrapper-scoped `LD_LIBRARY_PATH`). | Rule: full details in `docs/linux-flatpak-gpu-setup.md`; verify inside the sandbox with `flatpak run --command=sh` and `ldd` first.
- 2026-04-12 | Pattern: A setting that can disable GPU usage should not be labeled as generic "hardware acceleration" with a CPU option under it. | Rule: when the disabled state means CPU execution, label the control around GPU usage or backend selection so the UI does not imply CPU is still an acceleration mode.
- 2026-04-13 | Pattern: The model browser accumulated bugs because it has too many interacting states (download progress, cancel, select, remove, install-while-browsing) managed through custom rendering and snapshot patching. When you can't explain what a UI is supposed to do during concurrent operations, the design has outgrown its clarity. | Rule: before fixing bugs in complex UI, write down the allowed states and transitions. If the state machine is unclear, simplify the UI surface before adding fixes. Prefer Obsidian-native components (D-005) and reduce concurrent interactive states to what the user actually needs.
