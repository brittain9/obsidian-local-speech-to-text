- 2026-04-11 | [build] | Pattern: `cargo clippy` can redundantly re-run `whisper-rs-sys` native setup and fail in environments where the real `cargo build` / `cargo test` path already succeeds. | Rule: keep real native verification in `cargo build` and `cargo test`, and run clippy with `DOCS_RS=1` so linting skips the extra whisper.cpp CMake pass. Do NOT also set `WHISPER_DONT_GENERATE_BINDINGS=1` — that forces the shipped (Linux/glibc) bindings and breaks Windows with E0080 overflows on `_G_fpos_t` / `_IO_FILE` size asserts.
- 2026-04-12 | [test] | Pattern: installer `poll_event_returns_failed_on_channel_disconnect` flakes under parallel load (real HTTP, 10s timeout). Passes reliably in isolation. | Rule: rerun once before treating as a regression; fix or quarantine if it persists past 2026-Q3.
- 2026-04-13 | [ui] | Pattern: complex Obsidian UI surfaces (model browser) accumulate bugs when concurrent states aren't enumerated. | Rule: before patching multi-state UI, list allowed states/transitions; prefer Obsidian-native components (D-005) over custom rendering.
- 2026-04-19 | [process] | Pattern: Updating `PLANS.md` just because implementation changed creates extra churn and second-guesses an approved plan artifact. | Rule: when shipping code changes, do not edit `PLANS.md` unless the user explicitly asks for a plan change or the plan itself is being revised.
- 2026-04-19 | [ui] | Pattern: simplifying dictation chrome removed both the placement cue and the backend-progress cue at once, leaving the default `at_cursor` flow with no feedback. | Rule: keep at least one always-visible placement cue and one backend-progress cue when reducing dictation affordances.
- 2026-04-21 | [process] | Pattern: Stacked-branch merge status is easy to misread when a base branch was merged to `main` earlier and then advanced again afterward. | Rule: when confirming whether work reached `main`, compare the specific commit SHA against `origin/main`, not just whether the branch name was previously merged.
- 2026-04-21 | [process] | Pattern: A decimal Windows exit code diagnosed from memory produces wrong hypotheses. | Rule: always convert Windows process exit codes to hex before forming a crash hypothesis.
- 2026-04-21 | [build] | Pattern: whisper.cpp/ggml defaults `GGML_NATIVE=ON`, baking the build host's SIMD level (AVX-512 on CI runners) into the binary and crashing user machines without that ISA with `STATUS_ILLEGAL_INSTRUCTION`. The first fix at step-level `env:` scope was invisible to `Swatinem/rust-cache`, which keys before the build step runs and re-served stale artifacts. | Rule: release pipelines wrapping ggml MUST set `GGML_NATIVE=OFF` AT JOB SCOPE so the binary targets a portable baseline (AVX2) and the cache action sees the env when keying. Add `GGML WHISPER` to `Swatinem/rust-cache`'s `env-vars:` prefix list so future flag flips bust the cache key automatically.
- 2026-04-21 | [build] | Pattern: shipping a CUDA sidecar requires bundling toolkit DLLs alongside the binary — `cudart`/`cublas`/`cublasLt` are major-versioned, so a mismatched user CUDA gives `STATUS_DLL_NOT_FOUND`, and on Linux the CI runner's absolute `/usr/local/cuda/lib64/` rpath doesn't exist on user machines. ORT's CUDA provider DLL also transitively needs `cufft` and `cudnn` — invisible from the main exe's import table, so the first audit missed them and the sidecar silently fell back to CPU. | Rule: bundle `cudart`, `cublas`, `cublasLt`, and `cufft` alongside the binary in CUDA release archives (NVIDIA EULA Attachment A permits redistribution); set the Linux rpath to `$ORIGIN`. Audit every ONNX Runtime provider DLL's imports (`dumpbin /dependents onnxruntime_providers_<ep>.dll`) before each CUDA release, not just the main binary.
- 2026-04-22 | [process] | Pattern: `handleUninstallCuda` deleted `bin/cuda` before stopping the running sidecar, failing on Windows with `EBUSY` because the OS holds handles on loaded DLLs. Linux/macOS mask this — unlinking an open file succeeds there. | Rule: on Windows, any flow that deletes files owned by a live child process must `shutdown()` (or terminate + wait-for-exit) the process first. This applies to both the uninstall path and the upgrade `rm(destinationDirectory)` in `installSidecar`.
