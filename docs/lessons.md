- 2026-04-11 | [build] | Pattern: `cargo clippy` can rerun `whisper-rs-sys` setup and fail even when real build/test paths pass. | Rule: verify native code with `cargo build`/`cargo test`; run clippy with `DOCS_RS=1`, and do not set `WHISPER_DONT_GENERATE_BINDINGS=1` because shipped bindings break Windows.
- 2026-04-12 | [test] | Pattern: installer `poll_event_returns_failed_on_channel_disconnect` flakes under parallel load (real HTTP, 10s timeout). Passes reliably in isolation. | Rule: rerun once before treating as a regression; fix or quarantine if it persists past 2026-Q3.
- 2026-04-13 | [ui] | Pattern: complex Obsidian UI surfaces (model browser) accumulate bugs when concurrent states aren't enumerated. | Rule: before patching multi-state UI, list allowed states/transitions; prefer Obsidian-native components (D-005) over custom rendering.
- 2026-04-19 | [process] | Pattern: Updating `PLANS.md` just because implementation changed creates extra churn and second-guesses an approved plan artifact. | Rule: when shipping code changes, do not edit `PLANS.md` unless the user explicitly asks for a plan change or the plan itself is being revised.
- 2026-04-19 | [ui] | Pattern: simplifying dictation chrome removed both the placement cue and the backend-progress cue at once, leaving the default `at_cursor` flow with no feedback. | Rule: keep at least one always-visible placement cue and one backend-progress cue when reducing dictation affordances.
- 2026-04-21 | [process] | Pattern: Stacked-branch merge status is easy to misread when a base branch was merged to `main` earlier and then advanced again afterward. | Rule: when confirming whether work reached `main`, compare the specific commit SHA against `origin/main`, not just whether the branch name was previously merged.
- 2026-04-21 | [process] | Pattern: Windows crash triage went wrong when a decimal exit code was diagnosed from memory. | Rule: convert Windows process exit codes to hex before forming a crash hypothesis.
- 2026-04-21 | [build] | Pattern: release sidecars inherited runner SIMD and shipped AVX-512, crashing on non-AVX512 user CPUs. | Rule: set `GGML_NATIVE=OFF` and other native-probing env at release scope; never assume CI CPU features match user CPUs.
- 2026-04-21 | [build] | Pattern: a step-scoped `GGML_NATIVE=OFF` fix missed `rust-cache` restore keys and reused stale AVX-512 artifacts. | Rule: set build-affecting env at job/workflow scope and include relevant prefixes such as `GGML`/`WHISPER` in cache key env vars.
- 2026-04-21 | [build] | Pattern: CUDA sidecars depended on host CUDA 12 runtime libraries and failed on machines with incompatible toolkit installs. | Rule: bundle reviewed CUDA runtime libraries in CUDA archives and set Linux rpath to `$ORIGIN`.
- 2026-04-22 | [build] | Pattern: auditing only the sidecar exe missed ONNX Runtime CUDA provider dependencies such as cuFFT and cuDNN. | Rule: audit provider DLL/SO dependency chains, not just the main binary, before declaring CUDA packaging complete.
- 2026-04-22 | [process] | Pattern: deleting a running CUDA sidecar directory failed on Windows because loaded DLLs kept file handles open. | Rule: stop the owning sidecar before uninstalling or overwriting its binary directory.
- 2026-04-24 | [build] | Pattern: default CUDA architecture expansion made release builds slow and noisy. | Rule: pin the supported GPU baseline with `CMAKE_CUDA_ARCHITECTURES`, emit cargo timings, and keep logs bounded with `CMAKE_CUDA_FLAGS=-t0`; use full `CARGO_VERBOSE=1` only for temporary diagnostics.
- 2026-04-25 | [process] | Pattern: release docs used placeholder semver-like wording despite date-version tags. | Rule: document date-version tags with concrete examples such as `2026.4.25`, matching `manifest.version` exactly.
- 2026-04-25 | [process] | Pattern: architecture brainstorming jumped into code-shaped contracts before the user had aligned on the high-level system shape. | Rule: for architecture discussions, start with plain-language responsibilities and state flow before showing structs, traits, or protocol details.
- 2026-04-26 | [process] | Pattern: greenfield architecture planning introduced protocol versioning and compatibility bridges despite explicit no-shim direction. | Rule: when replacing a greenfield internal contract, plan a coordinated replacement across boundaries; do not add version negotiation, compatibility parsers, synthesized IDs, or fallback paths unless the user explicitly asks for backwards compatibility.
- 2026-04-26 | [process] | Pattern: implementation PRs omitted approved planning and decision artifacts after treating them as separate from code. | Rule: when a plan explicitly spans code, decisions, lessons, and rollout notes, include the relevant docs in the same PR unless the user asks to split them.
- 2026-04-27 | [build] | Pattern: feeding raw note prose to whisper's `initial_prompt` caused style-imitation hallucinations and silent token-budget truncation past 224 tokens. | Rule: shape whisper prompts as a `Glossary: t1, t2, ...` of distinctive terms (proper nouns, acronyms, code identifiers) sized well under 224 tokens; never pass narrative prose as context.
